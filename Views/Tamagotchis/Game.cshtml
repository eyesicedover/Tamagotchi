<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Tamagotchi Game</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  </head>
  <body>
    <div class="jumbotron text-center">
      <h1>Tamagotchi Game</h1>
    </div>
    <div class="well text-center">
      <p>Rules: Your Tamas have food, play, and sleep stats. Keep your Tamas happy by keeping their stats up! If any one stat drops below 0, that Tama will die. :'(</p>
      <br><br>
      @foreach (var Tama in Model)
      {
        if(Tama.GetDead() == false)
        {
          <div class="panel text-center">
            <div class="panel-heading">
              <h3>@Tama.GetName()</h3>
            </div>
            <p>Food: @Tama.GetFood()</p>
            <p>Play: @Tama.GetPlay()</p>
            <p>Sleep: @Tama.GetSleep()</p>
          </div>
        }
        else
        {
          <div class="panel text-center">
            <div class="panel-heading">
              <h3>@Tama.GetName() is DEAD!</h3>
            </div>
            <form action='/tamagotchis/remove/@Tama.GetId()' method="post">
              <button type='submit' class='btn btn-danger'>Remove This Tama</button>
            </form>
          </div>
        }
      }
      <form action='/tamagotchis/feed' method="post">
        <button type='submit' class='btn btn-primary'>Feed Them</button>
      </form><br>
      <form action='/tamagotchis/play' method="post">
        <button type='submit' class='btn btn-primary'>Play With Them</button>
      </form><br>
      <form action='/tamagotchis/sleep' method="post">
        <button type='submit' class='btn btn-primary'>Let Them Sleep</button>
      </form><br>
      <form action='/tamagotchis/wait' method="post">
        <button type='submit' class='btn btn-primary'>Wait</button>
      </form><br>
      <form action='/tamagotchis/clear' method="post">
        <button type='submit' class='btn btn-success'>Clear All Tamas</button>
      </form>
      <h4><a href="/tamagotchis">Go To Home Page</a></h4>
    </div>
  </body>
</html>
